/** A light recipe line is represented as a tuple of ingredient name and quantity. */
export type LightRecipeLine = [string, number];

/** A light recipe is represented as an array of ingredient name and quantity pairs. */
export type LightRecipe = LightRecipeLine[];
