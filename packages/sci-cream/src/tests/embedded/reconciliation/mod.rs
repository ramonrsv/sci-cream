//! Label-reconciliation suites: each validates an ingredient's modeled composition against a
//! published per-serving nutrition label, often with volume and density estimations.

mod flavourings;
mod util;
